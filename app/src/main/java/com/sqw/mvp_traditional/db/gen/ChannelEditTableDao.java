package com.sqw.mvp_traditional.db.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.sqw.mvp_traditional.db.table.ChannelEditTable;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CHANNEL_EDIT_TABLE".
*/
public class ChannelEditTableDao extends AbstractDao<ChannelEditTable, Void> {

    public static final String TABLENAME = "CHANNEL_EDIT_TABLE";

    /**
     * Properties of entity ChannelEditTable.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property ChannelName = new Property(0, String.class, "channelName", false, "CHANNEL_NAME");
        public final static Property ChannelId = new Property(1, String.class, "channelId", false, "CHANNEL_ID");
        public final static Property IsEnable = new Property(2, Integer.class, "isEnable", false, "IS_ENABLE");
        public final static Property Position = new Property(3, Integer.class, "position", false, "POSITION");
    }


    public ChannelEditTableDao(DaoConfig config) {
        super(config);
    }
    
    public ChannelEditTableDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CHANNEL_EDIT_TABLE\" (" + //
                "\"CHANNEL_NAME\" TEXT," + // 0: channelName
                "\"CHANNEL_ID\" TEXT," + // 1: channelId
                "\"IS_ENABLE\" INTEGER," + // 2: isEnable
                "\"POSITION\" INTEGER);"); // 3: position
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CHANNEL_EDIT_TABLE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ChannelEditTable entity) {
        stmt.clearBindings();
 
        String channelName = entity.getChannelName();
        if (channelName != null) {
            stmt.bindString(1, channelName);
        }
 
        String channelId = entity.getChannelId();
        if (channelId != null) {
            stmt.bindString(2, channelId);
        }
 
        Integer isEnable = entity.getIsEnable();
        if (isEnable != null) {
            stmt.bindLong(3, isEnable);
        }
 
        Integer position = entity.getPosition();
        if (position != null) {
            stmt.bindLong(4, position);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ChannelEditTable entity) {
        stmt.clearBindings();
 
        String channelName = entity.getChannelName();
        if (channelName != null) {
            stmt.bindString(1, channelName);
        }
 
        String channelId = entity.getChannelId();
        if (channelId != null) {
            stmt.bindString(2, channelId);
        }
 
        Integer isEnable = entity.getIsEnable();
        if (isEnable != null) {
            stmt.bindLong(3, isEnable);
        }
 
        Integer position = entity.getPosition();
        if (position != null) {
            stmt.bindLong(4, position);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public ChannelEditTable readEntity(Cursor cursor, int offset) {
        ChannelEditTable entity = new ChannelEditTable( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // channelName
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // channelId
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // isEnable
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3) // position
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ChannelEditTable entity, int offset) {
        entity.setChannelName(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setChannelId(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setIsEnable(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setPosition(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(ChannelEditTable entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(ChannelEditTable entity) {
        return null;
    }

    @Override
    public boolean hasKey(ChannelEditTable entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
